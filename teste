#include <stdio.h>
#include <stdlib.h>

int main(){
	int v1, v2;
	printf("Entre com o primeiro valor: ");
	scanf("%d",&v1);
	printf("Entre com o segundo valor: ");
	scanf("%d", &v2);
	printf("A soma dos dois valores eh: %d",v1+v2);
	system("pause");
}
